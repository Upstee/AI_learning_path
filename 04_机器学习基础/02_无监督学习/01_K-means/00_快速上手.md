# K-means快速上手

> **目标**：30分钟内快速体验K-means，建立学习信心，明确学习目标

---

## 🚀 30分钟快速体验

### 步骤1：安装依赖（2分钟）

```bash
pip install numpy pandas matplotlib scikit-learn
```

### 步骤2：运行第一个K-means示例（5分钟）

```python
from sklearn.cluster import KMeans
from sklearn.datasets import make_blobs
import matplotlib.pyplot as plt
import numpy as np

# 生成示例数据（300个点，3个簇）
X, y_true = make_blobs(n_samples=300, centers=3, n_features=2, random_state=42)

# 使用K-means聚类（只需要3行代码！）
kmeans = KMeans(n_clusters=3, random_state=42)
kmeans.fit(X)
labels = kmeans.labels_

# 可视化结果
plt.figure(figsize=(10, 5))

# 真实标签
plt.subplot(1, 2, 1)
plt.scatter(X[:, 0], X[:, 1], c=y_true, cmap='viridis', alpha=0.6)
plt.title('真实标签（3个簇）')
plt.xlabel('特征1')
plt.ylabel('特征2')

# K-means聚类结果
plt.subplot(1, 2, 2)
plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis', alpha=0.6)
plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1],
           c='red', marker='x', s=200, linewidths=3, label='质心')
plt.title('K-means聚类结果')
plt.xlabel('特征1')
plt.ylabel('特征2')
plt.legend()

plt.tight_layout()
plt.show()

print(f"✅ 成功！数据被分成 {len(np.unique(labels))} 个簇")
print(f"质心位置:\n{kmeans.cluster_centers_}")
```

**运行结果**：你会看到数据被成功分成3个簇，红色X标记是质心位置。

### 步骤3：修改参数看效果（10分钟）

尝试修改以下参数，观察效果：

```python
# 修改K值（簇的数量）
for k in [2, 3, 4, 5]:
    kmeans = KMeans(n_clusters=k, random_state=42)
    kmeans.fit(X)
    labels = kmeans.labels_
    
    plt.figure(figsize=(8, 6))
    plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis', alpha=0.6)
    plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1],
               c='red', marker='x', s=200, linewidths=3)
    plt.title(f'K={k} 的聚类结果')
    plt.show()
```

**观察**：
- K=2：数据被分成2个簇
- K=3：数据被分成3个簇（最合适）
- K=4：数据被分成4个簇（可能过拟合）
- K=5：数据被分成5个簇（明显过拟合）

### 步骤4：应用到真实场景（10分钟）

```python
import pandas as pd
from sklearn.preprocessing import StandardScaler

# 模拟客户数据
np.random.seed(42)
data = {
    '消费金额': np.random.normal(1000, 300, 200),
    '消费频率': np.random.poisson(5, 200),
    '最近消费': np.random.randint(0, 365, 200)
}
df = pd.DataFrame(data)

# 标准化数据（重要！）
scaler = StandardScaler()
X_scaled = scaler.fit_transform(df)

# K-means聚类
kmeans = KMeans(n_clusters=3, random_state=42)
kmeans.fit(X_scaled)
df['客户群体'] = kmeans.labels_

# 分析每个群体的特征
print("客户分群结果：")
for i in range(3):
    group = df[df['客户群体'] == i]
    print(f"\n群体{i} (共{len(group)}人):")
    print(f"  平均消费金额: {group['消费金额'].mean():.2f}")
    print(f"  平均消费频率: {group['消费频率'].mean():.2f}")
    print(f"  平均最近消费: {group['最近消费'].mean():.2f}天前")
```

**结果**：客户被分成3个群体，每个群体有不同的消费特征！

---

## 🎯 理解K-means在做什么

### 直观理解

**K-means就像"找K个中心点"**：
1. 随机选择K个中心点（质心）
2. 把每个数据点分配到最近的中心点
3. 重新计算中心点位置
4. 重复步骤2-3，直到中心点不再变化

**类比**：
- 就像把学生分成K个班级，每个班级有一个"班长"（质心）
- 每个学生选择最近的班长
- 班长位置会根据学生位置调整
- 直到班级稳定下来

### 可视化理解

运行上面的代码，你会看到：
- **数据点**：不同颜色代表不同的簇
- **质心**：红色X标记，是每个簇的中心
- **分配**：每个点被分配到最近的质心

---

## 📋 建立学习目标

完成快速上手后，你应该：

### ✅ 已经能做到
- [x] 使用scikit-learn运行K-means
- [x] 理解K-means的基本效果
- [x] 知道如何修改参数

### 🎯 接下来要学
- [ ] **理解原理**：K-means是如何工作的？
- [ ] **从零实现**：不依赖库，自己实现K-means
- [ ] **K值选择**：如何选择最佳的K值？
- [ ] **优化方法**：K-means++、Mini-batch等改进
- [ ] **实际应用**：在真实项目中如何使用？

---

## 💡 学习建议

1. **先体验再学习**：你已经运行了代码，看到了效果，现在学习原理会更容易理解
2. **多动手实验**：修改参数，观察效果，加深理解
3. **循序渐进**：先理解基本概念，再深入细节
4. **理论实践结合**：每学一个概念，立即用代码验证

---

## ⚠️ 常见问题

**Q: 为什么需要标准化数据？**
A: K-means基于距离计算，如果特征量纲不同（如金额和频率），距离计算会不准确。

**Q: 如何选择K值？**
A: 可以使用肘部法则或轮廓系数，后续会详细学习。

**Q: K-means一定能找到最佳结果吗？**
A: 不一定，K-means可能陷入局部最优，可以使用K-means++初始化或多次运行。

---

**准备好了吗？现在开始正式学习K-means的原理和实现！** 🚀
