# DBSCAN常见问题FAQ

> **目的**：快速解决学习过程中的常见问题

---

## 概念理解问题

### Q1: DBSCAN和K-means有什么区别？

**A**: 主要区别：

| 特性 | DBSCAN | K-means |
|------|--------|---------|
| **K值** | 不需要预设 | 需要预设 |
| **簇形状** | 任意形状 | 只能球形 |
| **异常值** | 自动识别 | 敏感 |
| **密度** | 基于密度 | 基于距离 |
| **适用场景** | 非球形簇、异常检测 | 球形簇、大数据 |

---

### Q2: 如何选择eps参数？

**A**: 使用k-距离图：

```python
from sklearn.neighbors import NearestNeighbors
import numpy as np

# 计算k-距离
k = min_samples - 1
neighbors = NearestNeighbors(n_neighbors=k)
neighbors_fit = neighbors.fit(X)
distances, indices = neighbors_fit.kneighbors(X)

# 取第k个最近邻的距离
k_distances = distances[:, -1]
k_distances = np.sort(k_distances)[::-1]

# 绘制k-距离图
plt.plot(k_distances)
plt.xlabel('点（按距离排序）')
plt.ylabel(f'{k}-距离')
plt.title('k-距离图（选择eps）')
plt.axhline(y=eps, color='r', linestyle='--', label=f'eps={eps}')
plt.legend()
plt.show()

# 选择"肘部"点作为eps
```

---

### Q3: 如何选择min_samples参数？

**A**: 
- **默认值**：通常设为2-5
- **高维数据**：设为维度+1
- **有噪声数据**：可以增大

---

## 代码实现问题

### Q4: 如何从零实现DBSCAN？

**A**: 核心步骤：

```python
def dbscan(X, eps, min_samples):
    n = len(X)
    labels = np.full(n, -1)  # -1表示未分类
    cluster_id = 0
    
    for i in range(n):
        if labels[i] != -1:  # 已分类
            continue
        
        # 找到邻居
        neighbors = find_neighbors(X, i, eps)
        
        if len(neighbors) < min_samples:  # 噪声点
            labels[i] = -1
            continue
        
        # 新簇
        labels[i] = cluster_id
        expand_cluster(X, labels, i, neighbors, cluster_id, eps, min_samples)
        cluster_id += 1
    
    return labels
```

---

## 实际应用问题

### Q5: DBSCAN在哪些场景中应用？

**A**: 
1. **异常检测**：自动识别异常点
2. **非球形簇**：处理月牙形、环形等簇
3. **地理热点分析**：发现地理聚集区域
4. **图像分割**：基于密度的图像分割

更多场景请参考：[实战场景库.md](./实战场景库.md)

---

## 错误排查

### Q6: 所有点都被标记为噪声

**A**: 
- **原因**：eps太小或min_samples太大
- **解决**：增大eps或减小min_samples

### Q7: 所有点都在一个簇

**A**: 
- **原因**：eps太大
- **解决**：减小eps

---

## 📖 更多资源

- **快速上手**：[00_快速上手.md](./00_快速上手.md)
- **学习检查点**：[学习检查点.md](./学习检查点.md)
- **实战场景库**：[实战场景库.md](./实战场景库.md)

---

**如果这里没有你遇到的问题，请查看其他资源！** 💪
