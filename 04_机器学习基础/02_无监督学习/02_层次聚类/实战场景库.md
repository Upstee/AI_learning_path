# 层次聚类实战场景库

> **目的**：提供真实业务场景的应用案例，帮助理解层次聚类的实际应用

---

## 📚 目录

- [常见应用场景](#常见应用场景)
- [真实项目案例](#真实项目案例)
- [算法对比表](#算法对比表)
- [面试常见问题](#面试常见问题)

---

## 常见应用场景

### 场景1：生物信息学 - 基因聚类

**业务背景**：
- 根据基因表达数据将基因分成不同功能组
- 用于发现基因功能和调控网络

**数据特征**：
- 基因表达数据（每个基因在不同条件下的表达量）
- 特征维度：条件数量（通常几十到几百维）

**实现步骤**：
```python
import pandas as pd
from scipy.cluster.hierarchy import dendrogram, linkage, fcluster
import matplotlib.pyplot as plt

# 1. 加载基因表达数据
# 行：基因，列：条件
gene_data = pd.read_csv('gene_expression.csv', index_col=0)

# 2. 计算链接矩阵
linkage_matrix = linkage(gene_data, method='ward', metric='euclidean')

# 3. 绘制树状图
plt.figure(figsize=(15, 10))
dendrogram(linkage_matrix, 
           labels=gene_data.index,
           leaf_rotation=90,
           leaf_font_size=8)
plt.title('基因聚类树状图')
plt.xlabel('基因')
plt.ylabel('距离')
plt.tight_layout()
plt.show()

# 4. 从树状图选择K值（例如K=5）
labels = fcluster(linkage_matrix, t=5, criterion='maxclust')

# 5. 分析每个簇的功能
for i in range(1, 6):
    cluster_genes = gene_data.index[labels == i]
    print(f"簇{i}: {len(cluster_genes)}个基因")
    # 进行功能富集分析...
```

**应用价值**：
- 发现共表达基因
- 识别功能模块
- 理解基因调控网络

---

### 场景2：文档主题聚类

**业务背景**：
- 将新闻文章或文档自动分成不同主题
- 用于内容管理和推荐

**数据特征**：
- 文档的TF-IDF向量
- 特征维度：词汇表大小（通常几千维）

**实现步骤**：
```python
from sklearn.feature_extraction.text import TfidfVectorizer
from scipy.cluster.hierarchy import dendrogram, linkage, fcluster

# 1. 文本向量化
vectorizer = TfidfVectorizer(max_features=1000)
X = vectorizer.fit_transform(documents).toarray()

# 2. 降维（可选，高维数据建议降维）
from sklearn.decomposition import PCA
pca = PCA(n_components=50)
X_reduced = pca.fit_transform(X)

# 3. 层次聚类
linkage_matrix = linkage(X_reduced, method='ward')

# 4. 绘制树状图
plt.figure(figsize=(15, 8))
dendrogram(linkage_matrix, 
           truncate_mode='level',
           p=10)
plt.title('文档主题聚类树状图')
plt.xlabel('文档索引')
plt.ylabel('距离')
plt.show()

# 5. 选择K值并获取标签
labels = fcluster(linkage_matrix, t=5, criterion='maxclust')

# 6. 分析每个主题
for i in range(1, 6):
    topic_docs = [documents[j] for j in range(len(documents)) if labels[j] == i]
    print(f"主题{i}: {len(topic_docs)}篇文章")
```

**应用价值**：
- 自动文档分类
- 内容推荐
- 信息检索

---

### 场景3：社交网络 - 社区发现

**业务背景**：
- 在社交网络中发现社区结构
- 用于用户推荐和内容分发

**数据特征**：
- 用户关系网络（邻接矩阵或边列表）
- 特征：用户特征或网络结构特征

**实现步骤**：
```python
import networkx as nx
from sklearn.cluster import AgglomerativeClustering

# 1. 构建网络图
G = nx.read_edgelist('social_network.edgelist')

# 2. 提取节点特征（例如：度中心性、介数中心性等）
features = []
for node in G.nodes():
    degree = G.degree(node)
    betweenness = nx.betweenness_centrality(G)[node]
    features.append([degree, betweenness])

X = np.array(features)

# 3. 层次聚类
clustering = AgglomerativeClustering(n_clusters=5, linkage='ward')
labels = clustering.fit_predict(X)

# 4. 可视化网络和社区
pos = nx.spring_layout(G)
colors = [labels[list(G.nodes()).index(node)] for node in G.nodes()]

plt.figure(figsize=(12, 8))
nx.draw(G, pos, node_color=colors, cmap='viridis', 
        with_labels=False, node_size=50, alpha=0.6)
plt.title('社交网络社区发现')
plt.show()
```

**应用价值**：
- 发现用户群体
- 精准推荐
- 内容分发

---

### 场景4：系统发育树（Phylogenetic Tree）

**业务背景**：
- 根据物种特征构建进化树
- 用于生物分类和进化研究

**数据特征**：
- 物种的形态特征或基因序列
- 特征维度：特征数量

**实现步骤**：
```python
from scipy.cluster.hierarchy import dendrogram, linkage
from scipy.spatial.distance import pdist, squareform

# 1. 加载物种特征数据
species_data = pd.read_csv('species_features.csv', index_col=0)

# 2. 计算距离矩阵
distances = pdist(species_data, metric='euclidean')
distance_matrix = squareform(distances)

# 3. 层次聚类
linkage_matrix = linkage(species_data, method='ward')

# 4. 绘制系统发育树
plt.figure(figsize=(15, 10))
dendrogram(linkage_matrix,
           labels=species_data.index,
           orientation='left',
           leaf_font_size=10)
plt.title('系统发育树')
plt.xlabel('距离')
plt.ylabel('物种')
plt.tight_layout()
plt.show()
```

**应用价值**：
- 物种分类
- 进化关系研究
- 生物多样性分析

---

### 场景5：图像分割

**业务背景**：
- 将图像分成不同区域
- 用于图像分析和计算机视觉

**数据特征**：
- 像素的RGB值和位置（5维：R, G, B, x, y）

**实现步骤**：
```python
from PIL import Image
import numpy as np

# 1. 加载图像
img = Image.open('image.jpg')
img_array = np.array(img)
h, w, c = img_array.shape

# 2. 提取特征（RGB + 位置）
features = []
for i in range(h):
    for j in range(w):
        r, g, b = img_array[i, j]
        features.append([r, g, b, i, j])

X = np.array(features)

# 3. 标准化（位置和颜色需要不同权重）
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# 4. 层次聚类（数据量大时使用采样）
from sklearn.utils import resample
X_sampled = resample(X_scaled, n_samples=min(5000, len(X_scaled)), random_state=42)

clustering = AgglomerativeClustering(n_clusters=5, linkage='ward')
labels_sampled = clustering.fit_predict(X_sampled)

# 5. 对剩余像素分配标签（使用KNN）
from sklearn.neighbors import KNeighborsClassifier
knn = KNeighborsClassifier(n_neighbors=1)
knn.fit(X_sampled, labels_sampled)
labels_all = knn.predict(X_scaled)

# 6. 可视化分割结果
segmented = labels_all.reshape(h, w)
plt.figure(figsize=(12, 5))
plt.subplot(1, 2, 1)
plt.imshow(img_array)
plt.title('原图像')
plt.subplot(1, 2, 2)
plt.imshow(segmented, cmap='viridis')
plt.title('分割结果')
plt.show()
```

**应用价值**：
- 图像分割
- 目标检测
- 图像分析

---

## 真实项目案例

### 案例1：基因表达数据分析系统

**项目描述**：
分析基因表达数据，将基因分成功能相关的簇，用于发现基因调控网络。

**技术栈**：
- Python, scipy, pandas, matplotlib
- 数据：1000个基因，50个条件

**实现要点**：
1. 数据预处理：标准化基因表达数据
2. 层次聚类：使用ward链接方法
3. 树状图分析：从树状图选择K值
4. 功能分析：对每个簇进行功能富集分析

**项目成果**：
- 发现5个主要功能模块
- 识别关键调控基因
- 构建基因调控网络

**参考代码**：[实战案例/简单项目1_基因聚类分析/](./实战案例/简单项目1_基因聚类分析/)

---

### 案例2：新闻主题聚类系统

**项目描述**：
自动将新闻文章分成不同主题，用于内容管理和推荐。

**技术栈**：
- Python, scikit-learn, jieba
- 数据：5000篇新闻文章

**实现要点**：
1. 文本预处理：分词、去停用词
2. 特征提取：TF-IDF向量化
3. 降维：PCA降维到50维
4. 层次聚类：生成主题树
5. 主题分析：提取关键词

**项目成果**：
- 自动分成10个主题
- 处理速度：500篇/分钟
- 主题准确率：80%

**参考代码**：[实战案例/简单项目2_文档主题聚类/](./实战案例/简单项目2_文档主题聚类/)

---

### 案例3：社交网络社区发现系统

**项目描述**：
在社交网络中发现用户社区，用于精准推荐。

**技术栈**：
- Python, networkx, scikit-learn
- 数据：1万用户，10万条边

**实现要点**：
1. 网络构建：构建用户关系图
2. 特征提取：提取网络特征
3. 层次聚类：发现社区结构
4. 可视化：可视化网络和社区

**项目成果**：
- 发现15个主要社区
- 推荐准确率提升25%
- 用户活跃度提升20%

**参考代码**：[实战案例/中等项目2_社交网络分析/](./实战案例/中等项目2_社交网络分析/)

---

## 算法对比表

### 什么时候用层次聚类？

| 场景 | 层次聚类 | K-means | DBSCAN | 推荐 |
|------|---------|---------|--------|------|
| **小数据（<1000）** | ✅ 适合 | ✅ 适合 | ✅ 适合 | 层次聚类（有树状图） |
| **中等数据（1000-10000）** | ⚠️ 较慢 | ✅ 适合 | ✅ 适合 | K-means或DBSCAN |
| **大数据（>10000）** | ❌ 太慢 | ✅ 适合 | ✅ 适合 | K-means或DBSCAN |
| **需要树状图** | ✅ 有 | ❌ 无 | ❌ 无 | 层次聚类 |
| **不知道K值** | ✅ 可从树状图选择 | ❌ 需要预设 | ✅ 不需要 | 层次聚类或DBSCAN |
| **需要层次结构** | ✅ 有 | ❌ 无 | ❌ 无 | 层次聚类 |

### 算法选择决策树

```
数据量是多少？
├── < 1000
│   ├── 需要树状图？ → 层次聚类 ✅
│   └── 不需要树状图？ → K-means或DBSCAN
├── 1000-10000
│   ├── 需要树状图？ → 层次聚类（较慢）⚠️
│   └── 不需要树状图？ → K-means或DBSCAN ✅
└── > 10000
    └── K-means或DBSCAN ✅
```

---

## 面试常见问题

### Q1: 解释层次聚类的工作原理

**标准答案**：
1. **初始化**：每个数据点是一个簇
2. **计算距离**：计算所有簇对之间的距离
3. **合并**：找到最近的两个簇，合并它们
4. **重复**：重复步骤2-3，直到达到目标K值或所有点合并

**加分点**：
- 提到两种方法（凝聚、分裂）
- 提到链接准则（ward、complete、average）
- 提到树状图的作用

---

### Q2: 如何从树状图选择K值？

**标准答案**：
1. **观察距离变化**：找到距离变化最大的点（肘部点）
2. **可视化选择**：在树状图上画水平线，观察切割点
3. **使用轮廓系数**：计算不同K值的轮廓系数，选择最大值

**加分点**：
- 提到具体实现方法
- 提到业务需求考虑
- 提到多种方法结合使用

---

### Q3: 层次聚类和K-means有什么区别？

**标准答案**：

**层次聚类**：
- 不需要预设K值
- 可以生成树状图
- 计算慢，O(n³)
- 适合小数据

**K-means**：
- 需要预设K值
- 计算快，O(n)
- 适合大数据
- 结果不可逆

**加分点**：
- 提到适用场景
- 提到可以结合使用
- 提到性能对比

---

### Q4: 什么是链接准则？有哪些常见方法？

**标准答案**：

**链接准则**：决定如何计算两个簇之间的距离

**常见方法**：
1. **Ward**：最小方差，产生紧凑簇
2. **Complete**：最远距离，对异常值鲁棒
3. **Average**：平均距离，平衡方法
4. **Single**：最近距离，可能产生链状簇

**加分点**：
- 提到每种方法的优缺点
- 提到选择建议
- 提到实际应用

---

### Q5: 如何优化层次聚类的性能？

**优化方法**：
1. **减少数据量**：采样或降维
2. **使用近似方法**：FastCluster库
3. **并行计算**：多进程处理
4. **提前停止**：达到目标K值后停止

**加分点**：
- 提到具体实现
- 提到性能提升指标
- 提到权衡考虑

---

## 📖 更多资源

- **代码示例**：[代码示例/](./代码示例/)
- **实战案例**：[实战案例/](./实战案例/)
- **常见问题**：[常见问题FAQ.md](./常见问题FAQ.md)
- **学习检查点**：[学习检查点.md](./学习检查点.md)

---

**希望这些场景能帮助你更好地理解层次聚类的实际应用！** 🚀
