# 层次聚类快速上手

> **目标**：30分钟内快速体验层次聚类，建立学习信心，明确学习目标

---

## 🚀 30分钟快速体验

### 步骤1：安装依赖（2分钟）

```bash
pip install numpy pandas matplotlib scikit-learn scipy
```

### 步骤2：运行第一个层次聚类示例（5分钟）

```python
from sklearn.cluster import AgglomerativeClustering
from sklearn.datasets import make_blobs
import matplotlib.pyplot as plt
from scipy.cluster.hierarchy import dendrogram, linkage
import numpy as np

# 生成示例数据（100个点，3个簇）
X, y_true = make_blobs(n_samples=100, centers=3, n_features=2, random_state=42)

# 使用层次聚类（只需要2行代码！）
clustering = AgglomerativeClustering(n_clusters=3)
labels = clustering.fit_predict(X)

# 可视化结果
plt.figure(figsize=(15, 5))

# 真实标签
plt.subplot(1, 3, 1)
plt.scatter(X[:, 0], X[:, 1], c=y_true, cmap='viridis', alpha=0.6)
plt.title('真实标签（3个簇）')
plt.xlabel('特征1')
plt.ylabel('特征2')

# 层次聚类结果
plt.subplot(1, 3, 2)
plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis', alpha=0.6)
plt.title('层次聚类结果')
plt.xlabel('特征1')
plt.ylabel('特征2')

# 树状图
plt.subplot(1, 3, 3)
linkage_matrix = linkage(X, method='ward')
dendrogram(linkage_matrix, truncate_mode='level', p=3)
plt.title('树状图（Dendrogram）')
plt.xlabel('样本索引')
plt.ylabel('距离')

plt.tight_layout()
plt.show()

print(f"✅ 成功！数据被分成 {len(np.unique(labels))} 个簇")
```

**运行结果**：你会看到数据被成功分成3个簇，以及一个树状图展示聚类过程！

### 步骤3：理解树状图（10分钟）

树状图是层次聚类的核心可视化工具：

```python
# 生成更大的数据集
X, _ = make_blobs(n_samples=50, centers=4, n_features=2, random_state=42)

# 计算链接矩阵
linkage_matrix = linkage(X, method='ward')

# 绘制完整树状图
plt.figure(figsize=(12, 8))
dendrogram(linkage_matrix, 
           truncate_mode='level', 
           p=5,
           show_leaf_counts=True,
           leaf_font_size=10)
plt.title('层次聚类树状图')
plt.xlabel('样本索引或（簇大小）')
plt.ylabel('距离')
plt.axhline(y=5, color='r', linestyle='--', label='切割线（K=4）')
plt.legend()
plt.show()

# 从树状图选择K值
# 在距离=5处切割，得到4个簇
clustering = AgglomerativeClustering(n_clusters=4, linkage='ward')
labels = clustering.fit_predict(X)

plt.figure(figsize=(8, 6))
plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis', alpha=0.6)
plt.title('从树状图选择K=4的聚类结果')
plt.xlabel('特征1')
plt.ylabel('特征2')
plt.show()
```

**观察**：
- 树状图展示了聚类的层次结构
- 从下往上，簇逐渐合并
- 可以通过"切割"树状图选择K值

### 步骤4：对比不同链接方法（10分钟）

```python
# 测试不同的链接方法
methods = ['ward', 'complete', 'average', 'single']

fig, axes = plt.subplots(2, 2, figsize=(15, 12))
axes = axes.flatten()

for idx, method in enumerate(methods):
    # 层次聚类
    clustering = AgglomerativeClustering(n_clusters=3, linkage=method)
    labels = clustering.fit_predict(X)
    
    # 可视化
    axes[idx].scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis', alpha=0.6)
    axes[idx].set_title(f'{method} 链接方法')
    axes[idx].set_xlabel('特征1')
    axes[idx].set_ylabel('特征2')

plt.tight_layout()
plt.show()
```

**观察**：
- **ward**：通常产生紧凑的球形簇
- **complete**：对异常值更鲁棒
- **average**：平衡的方法
- **single**：可能产生链状簇

---

## 🎯 理解层次聚类在做什么

### 直观理解

**层次聚类就像"逐步合并"**：
1. 开始：每个点是一个簇
2. 合并：找到最近的两个簇，合并它们
3. 重复：继续合并，直到只剩K个簇（或所有点合并成一个簇）

**类比**：
- 就像公司合并：小公司逐步合并成大公司
- 或者像家族树：从个人到家庭到家族

### 两种方法

**凝聚聚类（自底向上）**：
- 从每个点开始
- 逐步合并最近的簇
- 直到达到目标K值

**分裂聚类（自顶向下）**：
- 从所有点开始
- 逐步分裂成更小的簇
- 直到达到目标K值

**常用**：凝聚聚类（Agglomerative Clustering）

---

## 📋 建立学习目标

完成快速上手后，你应该：

### ✅ 已经能做到
- [x] 使用scikit-learn运行层次聚类
- [x] 理解树状图的基本含义
- [x] 知道如何选择K值（从树状图）

### 🎯 接下来要学
- [ ] **理解原理**：层次聚类是如何工作的？
- [ ] **从零实现**：不依赖库，自己实现层次聚类
- [ ] **链接准则**：不同链接方法的影响
- [ ] **树状图解读**：如何从树状图选择K值？
- [ ] **实际应用**：在真实项目中如何使用？

---

## 💡 学习建议

1. **先体验再学习**：你已经运行了代码，看到了效果，现在学习原理会更容易理解
2. **多观察树状图**：树状图是理解层次聚类的关键
3. **对比不同方法**：尝试不同的链接方法，观察效果
4. **与K-means对比**：理解两种算法的区别和适用场景

---

## ⚠️ 常见问题

**Q: 层次聚类和K-means有什么区别？**
A: 层次聚类不需要预设K值，可以生成树状图，但计算较慢；K-means需要预设K值，计算快速。

**Q: 如何从树状图选择K值？**
A: 在树状图上画一条水平线，切割点数量就是K值。选择距离变化最大的点。

**Q: 什么时候用层次聚类？**
A: 当数据量不大（<10000），需要树状图，或不知道K值时使用。

---

**准备好了吗？现在开始正式学习层次聚类的原理和实现！** 🚀
